/** Desktop width (px) of the Copilot rail. Single source of truth shared by
 *  CopilotPanel (its width) and AppShell (the space it reserves for the split). */
export const COPILOT_PANEL_WIDTH = 380;
