"use client";

/**
 * Top-nav rows in the My Views sidebar (Home / Map / Activities / Tasks /
 * Leaderboard).
 *
 * Phase B3 stub — the real implementation lands in B6 within this same
 * implementer pass. Keeping this as a function component referenced from
 * ViewsSidebar.tsx so B6's expansion is a single-file edit rather than a
 * fresh file + import dance.
 */
export default function SidebarTopNav() {
  return null;
}
